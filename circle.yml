## Customize the test machine
machine:

  timezone:
    America/Los_Angeles # Set the timezone

  # Version of python to use
  python:
    version: 3.4.3


dependencies:
  pre:
    - pip3 install nose

test:

  override:
    nosetests test/unit_tests.py